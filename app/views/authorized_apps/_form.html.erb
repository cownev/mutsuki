<%= form_for(@authorized_app) do |f| %>
  <% if @authorized_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authorized_app.errors.count, "error") %> prohibited this authorized_app from being saved:</h2>

      <ul>
      <% @authorized_app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
