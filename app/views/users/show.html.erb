<h1>Detail of <%= @user.name %></h1>

<table  class="table table-hover">
    <tr>
      <th>Name : </th>
      <td><%= @user.name %></td>
    </tr>
    <tr>
      <th>Gender : </th>
      <td><%= @user.gender %></td>
    </tr>
    <tr>
      <th>Birthday : </th>
      <td><%= @user.birthday %></td>
    </tr>
</table>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back to Lsit', users_path %>
